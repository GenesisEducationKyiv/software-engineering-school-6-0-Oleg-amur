database:
  connectionString: "postgres://postgres:postgres@localhost:5432/release_notifier?sslMode=disable"
server:
  host: "0.0.0.0"
  port: "8080"
  grpcPort: "50051"
eventBus:
  url: "amqp://guest:guest@localhost:5672/"
  notificationExchange: "notifications"
  notificationQueue: "notification-worker"
  notificationDLQ: "notification-worker.dlq"
  subscriptionSagaQueue: "release-notifier.subscription-saga"
  subscriptionSagaDLQ: "release-notifier.subscription-saga.dlq"
releaseTracker:
  url: "http://localhost:8081"
  timeout: "10s"
