database:
  connectionString: "postgres://postgres:postgres@localhost:5432/subscription_service?sslMode=disable"
server:
  host: "0.0.0.0"
  port: "8080"
  grpcPort: "50051"
eventBus:
  url: "amqp://guest:guest@localhost:5672/"
  notificationExchange: "notifications"
  notificationQueue: "notification-worker"
  notificationDLQ: "notification-worker.dlq"
  subscriptionSagaQueue: "subscription-service.subscription-saga"
  subscriptionSagaDLQ: "subscription-service.subscription-saga.dlq"
releaseTracker:
  url: "http://localhost:8081"
  timeout: "10s"
